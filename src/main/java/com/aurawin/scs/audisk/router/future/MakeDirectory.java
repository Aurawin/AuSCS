package com.aurawin.scs.audisk.router.future;

import java.util.concurrent.Callable;

public class MakeDirectory extends Isssue implements Callable{
    @Override
    public Long call() throws Exception{
        return 0l;
    }
}
